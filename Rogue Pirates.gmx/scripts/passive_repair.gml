playerHull += irandom( playerCrew / 10 );
if( playerHull > playerMaxHull ) playerHull = playerMaxHull;
playerRiggingHealth += irandom( playerCrew / 10 );
if( playerRiggingHealth > playerMaxRiggingHealth )playerRiggingHealth = playerMaxRiggingHealth;
